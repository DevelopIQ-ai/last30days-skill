Repair the contract drift the GetXAPI and Jev work left behind. `GETXAPI_KEY` is now listed in `scripts/setup-keychain.sh` and `scripts/setup-pass.sh`, so the Keychain and `pass` helpers cover every credential `lib/env.py` loads. `discover.py` honors `LAST30DAYS_MEMORY_DIR` for its default checkpoint path instead of hardcoding a fixed directory under the user's home.

Four source-assertion guards that the same work outgrew now assert structure rather than exact strings: the handle-lane capable set and the xquik/getxapi branch are matched by shape, the `LAST30DAYS_X_BACKEND` pin row is checked for "opt-ins last" rather than a fixed backend list, and the README translation check locates the navigation line instead of assuming a fixed offset. `LAST30DAYS_GETXAPI_EXACT_QUERY` and `LAST30DAYS_SKIP_RUN_CACHE` are recorded as process-only env vars, which is how both are actually read.
