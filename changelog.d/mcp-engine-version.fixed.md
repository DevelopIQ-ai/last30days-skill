Report the real version in reports rendered through the MCP server. The engine resolves its own version by walking up from `lib/render.py` for `.claude-plugin/plugin.json` (then `SKILL.md`); the extracted MCP cache holds `scripts/` content only, so neither existed and every MCP-rendered report announced itself as `last30days v?`. `sync-engine.sh` now vendors the plugin manifest alongside the engine, which fixes the lookup through the mechanism the engine already has — no engine change and no second place to record the version.
